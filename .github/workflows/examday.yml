name: examday
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build-run:
    runs-on: ubuntu-latest

    steps:

    - name: For Exam Purpose
      uses: actions/checkout@v4

    - name: Just To run the below command
      run: |
        echo "DevOps Exam Day"
        echo "GitHub Actions is working successfully!"
